version: '3.7'

services:
    app:
        build:
            context: .
            dockerfile: ./Dockerfile
        image: 'pruebaaravel'
        container_name: noexpapi
        ports:
            - "3700:8000"
        command: php artisan queue:work
networks: 
  default: 
    external: 
      name: raspisql_default 